import UIKit

class FirstViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .white // Adicionei uma cor de fundo padrão
    }
    @IBAction func secondPagePressedButton(_ sender: Any) {
        // Aqui você pode adicionar a navegação para a próxima página se necessário
    }
}
